Rerun the experiments in `blade-evaluation` to use Copilot CLI instead of Codex/Claude code, by making a folder called `blade-evaluation-copilot`.

- Once the experiments are setup, run the experiments.
First, run a single seed for 3 datasets and ensure that the custom setting is outperforming the standard setting
- If the custom setting is indeed outperforming the standard setting, proceed to run the full set of experiments across all seeds and datasets.

Finally update the results in `../paper-imodels-agentic/content.tex`. Also update all the writing in Sec 4.3 to reflect the new results obtained using Copilot CLI.
